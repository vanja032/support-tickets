export const handler = async(event) => {
    const response = {
        statusCode: 200,
        body: {
            message: "Support Tickets Platform info"
        },
    };
    return response;
};
